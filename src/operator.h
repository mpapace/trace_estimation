#ifndef OPERATOR_H
#define OPERATOR_H

unsigned int clover_site_size(unsigned int num_lattice_site_var, unsigned int depth);
unsigned int projection_buffer_size(unsigned int num_lattice_site_var,
                                    unsigned int num_lattice_site);

#endif // OPERATOR_H
